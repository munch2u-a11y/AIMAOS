# AIMAOS public-beta runtime dependencies (validated 2026-07-31).
# Direct dependencies are pinned so beta installations do not drift silently.
PyYAML==6.0.3
rich==15.0.0
ollama==0.6.2
requests==2.34.2
docxtpl==0.20.2
python-docx==1.2.0
pypdf==6.14.2
