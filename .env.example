# Copy values into your service manager or a private, git-ignored environment file.
# Required only when dashboard token authentication is desired (mandatory for remote proxy use).
AIMAOS_UI_TOKEN=

# A non-loopback bind is rejected unless an operator explicitly confirms a TLS proxy boundary.
AIMAOS_BEHIND_TLS_PROXY=0
