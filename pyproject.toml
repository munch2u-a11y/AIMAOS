[tool.pytest.ini_options]
addopts = "-ra --strict-markers"
testpaths = ["tests/unit"]

[tool.coverage.run]
branch = true
source = ["core", "aimaos_ui"]

[tool.ruff]
target-version = "py311"
line-length = 110
